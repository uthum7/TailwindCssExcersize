/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html",               // Root-level index.html file
    "./src/**/*.{js,ts,jsx,tsx}", // All JS, TS, JSX, and TSX files inside the src folder and subfolders
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
